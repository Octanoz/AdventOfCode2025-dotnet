namespace AdventUtilities;


/* public readonly struct TableContext(char[,] table, int maxRow, int maxCol)
{
    public char[,] Table { get; } = table;
    public int MaxRow { get; } = maxRow;
    public int MaxCol { get; } = maxCol;
}

public readonly struct MapContext(char[,] map, int maxX, int maxY)
{
    public char[,] Map { get; } = map;
    public int MaxX { get; } = maxX;
    public int MaxY { get; } = maxY;
} */